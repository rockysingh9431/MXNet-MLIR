using 2 passes

createLowerToAffinePass
createLowerToLLVMPass

main->calling some functions 
registerMLIRContextCLOptions ->
      egisterMLIRContextCLOptions allows you to control and configure 
      the behavior of the MLIRContext through command-line options (CLI). 
      This function is part of MLIR’s infrastructure to expose context-level
       configurations for debugging, error handling, and performance tuning.

registerPassManagerCLOptions -> Utility function that registers command line options
        for configuring the passManager.

main->loadAndProcessMLIR->loadMLIR

loadMLIR will load the module and return the module.

This module will  be passed to dumpLLVMIR to process Optimization and to
convert it to LLVMIR.





